import styled from "styled-components"

export const Container = styled.div`
    .section {
        margin-inline: auto;
        margin-top: 3.2rem;
        margin-bottom: 3.2rem;
        width: 100%;
        max-width: 80rem;

        .back {
            font-size: 2rem;
            margin-bottom: 1rem;
            width: max-content;
        }
    }
`